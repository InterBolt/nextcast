const SOURCE_EXTS = [".tsx", ".ts", ".jsx", ".js"];
const CONFIG_EXTS = ["js", "mjs", "cjs", "mts", "cts", "ts"];
const ROUTE_GROUP_REGEX = /^.*\(.*\)$/;

const nextConstants = {
  SOURCE_EXTS,
  CONFIG_EXTS,
  ROUTE_GROUP_REGEX,
};

export default nextConstants;
